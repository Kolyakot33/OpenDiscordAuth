Main : JavaPlugin -------------- Главный файл плагина
DiscordBot : ListenerAdapter --- Отвечает за дискорд бота
ServerEvents ------------------- Отвечает за обработку событий сервера
;cogs -------------------------- 'Винтики'
-Config ---- Отвечает за все параменты, данные туда должны подтягиватся из config.yml(Main.pluginConfig | Main.loadConfig())
-Utils ----- Различные утилиты для быстрой работы


-------
Нужно сделать | Логика:
00 При входе на сервер: проверяется наличие сессии
;Если сессия есть и IP совподает с текущим IP
    Написать об успешной авторизации

;Если (Сессии нету || Есть но IP НЕ совподает с текущий IP)
    Добавить человека в список не авторизованых
    Создать код
    Включить таймер (что бы человека выкидывало через некоторое время если он досихпор не залогинен)

00 При сообщении в дискорде
;Если сообщение написано в DM(личные сообщения) и (автор сообщения != this.user)
    ;;Если в tempCodes есть такой же как и сообщение
        Достать откуда-то ник игрока и залогинить его

    ;;Else
        Сказать что такого кода нету.


Сессии {

}
-------